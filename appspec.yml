version: 0.0
os: linux
files:
  - source:  /
    destination: /home/ubuntu/build/
    overwrite: yes
permissions:
  - object: /
    pattern: "**"
    owner: ubuntu
    group: ubuntu

# hooks:
#   ApplicationStart:
#     - location:  # ApplicationStart 단계에서 sh 실행
#       timeout: 60
#       runas: ubuntu
